---
// Ours
import { ALT_LANGUAGES } from "@/constants/lang";
import Layout from "@/layouts/layout.astro";
import Main from "@/components/main.astro";


export function getStaticPaths() {
  return ALT_LANGUAGES.map((lang) => ({ params: { locale: lang } }));
}
---


<Layout>
  <Main />
</Layout>
